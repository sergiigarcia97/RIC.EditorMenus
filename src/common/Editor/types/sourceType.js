/**
 * Posibles orígenes o destinos de transformación de JSON / carga de componentes
 * @enum {String} sourceType
 */
const sourceType = {
    VID: "VID",
    FORMS: "FORMS",
    CORE: "CORE"
}
export default sourceType;