/**
 * Posibles tipos de lista de opciones, usamos este tipo para saber en qué capa queremos guardar la lista de items
 */
const optionType = {
    HTML: "html",
    ATTR: "attribute",
    COLUMN: "column"
}
export default optionType;